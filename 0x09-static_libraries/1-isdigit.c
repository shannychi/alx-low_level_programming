#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _isdigit - checks for digit
 * @c: input
 * Return: Always 0 (success)
 */
int _isdigit(int c)
{
return (isdigit(c));
}
