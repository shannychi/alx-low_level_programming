#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _strcat - check code
 * @dest: input
 * @src: input
 * Return: Always 0 (success)
 */
char *_strcat(char *dest, char *src)
{
return (strcat(dest, src));
}
