#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strncat - check code
 * @dest: input
 * @src: input
 * @n: input
 * Return: Always 0 (success)
 */
char *_strncat(char *dest, char *src, int n)
{
return (strncat(dest, src, n));
}
