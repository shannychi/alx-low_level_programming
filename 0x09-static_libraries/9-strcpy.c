#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _strcpy - check code
 * @dest: input
 * @src: inpu
 * Return: Always 0 (success)
 */
char *_strcpy(char *dest, char *src)
{
return (strcpy(dest, src));
}
