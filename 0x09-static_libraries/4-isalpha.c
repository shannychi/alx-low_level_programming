#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isalpha - function that cheks for alphabet
 * @c: input
 * Return: Always 0 (success)
 */
int _isalpha(int c)
{
return (isalpha(c));
}
