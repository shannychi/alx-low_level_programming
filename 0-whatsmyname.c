#include <stdio.h>
#include "main.h"
/**
 * main - prints its name + \n
 * @argc: number of arugnment typed
 * @argv: array poniting the arugnment
 * Return: Always 0 (success)
 */
int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);
		return (0);
}
